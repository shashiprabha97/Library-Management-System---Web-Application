package com.library.system.repositories;

import com.library.system.Entity.GovPublish;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GovPublishRepository extends JpaRepository<GovPublish,String> {
}
